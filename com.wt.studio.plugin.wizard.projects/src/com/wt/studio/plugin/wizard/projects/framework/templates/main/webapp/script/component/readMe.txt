validation.js
	此组件是表单验证的组件，使用时需引用相应的css文件。
	具体使用方式：
	1、需要在验证的html标签中加入heavalidation这样的一个属性。
	2、需要在类选择其中添加class="validate[optional,custom[noSpecialCaracters],length[0,20]]" 
	3、其中noSpecialCaracters是在validation.js中写好的正则表达式对应的名称
